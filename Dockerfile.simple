FROM node:18-alpine

# Installer Directus globalt
RUN npm install -g directus

# Opret app mappe
WORKDIR /app

# Eksponer port
EXPOSE 10000

# Start Directus med eksplicit host og port
CMD ["directus", "start", "--host", "0.0.0.0", "--port", "10000"] 